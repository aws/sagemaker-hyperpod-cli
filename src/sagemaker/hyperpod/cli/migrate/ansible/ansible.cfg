[defaults]
inventory = ./inventory.generated.ini
host_key_checking = False
retry_files_enabled = False
stdout_callback = default
roles_path = ./roles
gathering = explicit
# HyperPod nodes are reached over SSM Session Manager, not SSH.
# The inventory sets ansible_connection=community.aws.aws_ssm per host.

[ssh_connection]
pipelining = True
