apiVersion: inference.sagemaker.aws.amazon.com/v1
kind: InferenceEndpointConfig
metadata:
  name: deepseek-15b
  namespace: default
spec:
  replicas: 1
  modelName: deepseek-15b
  endpointName: deepseek-15b
  instanceType: ml.p4d.24xlarge
  invocationEndpoint: v1/chat/completions
  modelSourceConfig:
    modelSourceType: s3
    s3Storage:
      bucketName: <YOUR_MODEL_BUCKET>
      region: us-west-2
    modelLocation: deepseek-15b
    prefetchEnabled: true
  metrics:
    enabled: true
    modelMetrics:
      port: 8000
  loadBalancer:
    healthCheckPath: /health
  worker:
    resources:
      requests:
        cpu: 11000m
        memory: 15Gi
        nvidia.com/gpu: 1
      limits:
        nvidia.com/gpu: 1
    image: lmcache/vllm-openai:v0.3.7
    environmentVariables:
      - name: PYTHONHASHSEED
        value: "123"
      - name: PROMETHEUS_MULTIPROC_DIR
        value: /tmp
      - name: VLLM_REQUEST_TIMEOUT
        value: "600"
      - name: MODEL_PORT
        value: "8000"
    args:
      - "/opt/ml/model"
      - "--port"
      - "8000"
      - "--max-model-len"
      - "8000"
    modelInvocationPort:
      containerPort: 8000
      name: http
    modelVolumeMount:
      name: model-weights
      mountPath: /opt/ml/model
    # Custom probes configuration for Scenario 3
    probes:
      # Liveness probe to check if container is alive
      livenessProbe:
        httpGet:
          path: /health
          port: 8000
        initialDelaySeconds: 60
        periodSeconds: 15
        timeoutSeconds: 5
        failureThreshold: 3
      # Readiness probe to check if container is ready to serve
      readinessProbe:
        httpGet:
          path: /health
          port: 8000
        initialDelaySeconds: 30
        periodSeconds: 10
        timeoutSeconds: 3
        successThreshold: 1
        failureThreshold: 3
      # Startup probe for slow-starting model loading
      startupProbe:
        httpGet:
          path: /health
          port: 8000
        initialDelaySeconds: 0
        periodSeconds: 20
        timeoutSeconds: 5
        failureThreshold: 30  # 30 * 20s = 10 minutes max startup time
